import { css } from '@emotion/core';

export const styWrapper = css`
  max-width: 100%;

  .main-page {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    height: calc(100vh - 4px);
    padding: 0 20px;
  }
`;
